[request_definition]
r = sub, obj, act, res, ctx

[policy_definition]
p = sub, obj, act, res

[role_definition]
g = _, _, _

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = g(r.sub, p.sub, r.ctx) && keyMatch(r.obj, p.obj) && keyMatch(r.act, p.act) && keyMatch(r.res, p.res)
